# 小程序迁移 - 快速参考

## 🚀 常用代码片段

### 路由跳转

```typescript
// 跳转到新页面
uni.navigateTo({
  url: '/pages/camp/camp'
})

// 带参数跳转
uni.navigateTo({
  url: '/pages/camp/camp?id=123&name=test'
})

// 替换当前页面
uni.redirectTo({
  url: '/pages/login/login'
})

// 返回上一页
uni.navigateBack()

// 返回首页
uni.reLaunch({
  url: '/pages/home/home'
})

// TabBar 页面跳转
uni.switchTab({
  url: '/pages/home/home'
})
```

### 页面参数接收

```typescript
import { onLoad } from '@dcloudio/uni-app'

onLoad((options) => {
  // options 包含页面参数
  const id = options.id
  const name = options.name
})
```

### API 请求

```typescript
// 封装后的请求方法
uni.request({
  url: 'http://26.83.153.194:8080/camp/dashboard',
  method: 'GET',
  header: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  },
  success: (res) => {
    console.log(res.data)
  },
  fail: (err) => {
    console.error(err)
  }
})
```

### 存储操作

```typescript
// 存储数据
uni.setStorageSync('token', tokenValue)

// 获取数据
const token = uni.getStorageSync('token')

// 移除数据
uni.removeStorageSync('token')

// 清空所有数据
uni.clearStorageSync()
```

### 页面生命周期

```typescript
import { onLoad, onShow, onReady, onHide, onUnload } from '@dcloudio/uni-app'

onLoad((options) => {
  // 页面加载时执行（类似 onMounted）
  // options 包含页面参数
})

onShow(() => {
  // 页面显示时执行（每次进入页面都会触发）
})

onReady(() => {
  // 页面初次渲染完成
})

onHide(() => {
  // 页面隐藏时执行
})

onUnload(() => {
  // 页面卸载时执行
})
```

### 设置导航栏标题

```typescript
uni.setNavigationBarTitle({
  title: '新标题'
})
```

### 显示提示信息

```typescript
// 成功提示
uni.showToast({
  title: '操作成功',
  icon: 'success',
  duration: 2000
})

// 错误提示
uni.showToast({
  title: '操作失败',
  icon: 'error',
  duration: 2000
})

// 加载提示
uni.showLoading({
  title: '加载中...'
})

// 隐藏加载提示
uni.hideLoading()
```

### 选择图片

```typescript
uni.chooseImage({
  count: 1,
  success: (res) => {
    const tempFilePath = res.tempFilePaths[0]
    // 上传图片
    uni.uploadFile({
      url: 'http://26.83.153.194:8080/api/upload',
      filePath: tempFilePath,
      name: 'file',
      header: {
        'Authorization': `Bearer ${token}`
      },
      success: (uploadRes) => {
        console.log('上传成功', uploadRes)
      }
    })
  }
})
```

### 音频播放

```typescript
const audio = uni.createInnerAudioContext()
audio.src = '/static/sounds/hit.mp3'
audio.play()

audio.onPlay(() => {
  console.log('开始播放')
})

audio.onError((res) => {
  console.error('播放失败', res)
})
```

## 🔄 代码对比

### 路由跳转

| Vue Router | uni-app |
|-----------|---------|
| `router.push('/camp')` | `uni.navigateTo({ url: '/pages/camp/camp' })` |
| `router.replace('/login')` | `uni.redirectTo({ url: '/pages/login/login' })` |
| `router.back()` | `uni.navigateBack()` |
| `router.push({ path: '/camp', query: { id: '123' } })` | `uni.navigateTo({ url: '/pages/camp/camp?id=123' })` |

### 生命周期

| Vue 3 | uni-app |
|-------|---------|
| `onMounted(() => {})` | `onLoad((options) => {})` |
| `onUnmounted(() => {})` | `onUnload(() => {})` |
| - | `onShow(() => {})` |
| - | `onHide(() => {})` |
| - | `onReady(() => {})` |

### 存储 API

| Web API | uni-app |
|---------|---------|
| `localStorage.setItem('key', 'value')` | `uni.setStorageSync('key', 'value')` |
| `localStorage.getItem('key')` | `uni.getStorageSync('key')` |
| `localStorage.removeItem('key')` | `uni.removeStorageSync('key')` |
| `localStorage.clear()` | `uni.clearStorageSync()` |

### 组件标签

| HTML | uni-app |
|------|---------|
| `<div>` | `<view>` |
| `<span>` | `<text>` |
| `<img src="...">` | `<image src="...">` |
| `<a href="...">` | `<navigator url="...">` |

### HTTP 请求

| Axios | uni.request |
|-------|-------------|
| `axios.get(url)` | `uni.request({ url, method: 'GET' })` |
| `axios.post(url, data)` | `uni.request({ url, method: 'POST', data })` |
| `axios.put(url, data)` | `uni.request({ url, method: 'PUT', data })` |
| `axios.delete(url)` | `uni.request({ url, method: 'DELETE' })` |

## 📦 常用组件

### 按钮

```vue
<button type="primary" @click="handleClick">按钮</button>
<button type="default">默认按钮</button>
<button type="warn">警告按钮</button>
```

### 输入框

```vue
<input v-model="value" placeholder="请输入" />
```

### 图片

```vue
<image src="/static/logo.png" mode="aspectFit" />
```

### 列表

```vue
<view v-for="item in list" :key="item.id">
  {{ item.name }}
</view>
```

## ⚠️ 常见问题

### 1. 页面跳转失败

**问题**：`uni.navigateTo` 报错 "page not found"

**解决**：
- 检查 `pages.json` 中是否配置了该页面
- 检查路径是否正确（必须以 `/pages/` 开头）
- 检查文件是否存在

### 2. 图片不显示

**问题**：图片路径正确但不显示

**解决**：
- 确保图片在 `static/` 目录下
- 使用 `<image>` 标签而不是 `<img>`
- 检查图片路径（相对路径或绝对路径）

### 3. API 请求失败

**问题**：网络请求失败

**解决**：
- 检查服务器域名是否在微信公众平台配置
- 检查是否使用 HTTPS（生产环境）
- 检查请求头是否正确

### 4. 样式不生效

**问题**：CSS 样式不生效

**解决**：
- 检查是否使用了小程序不支持的 CSS 属性
- 检查选择器是否正确
- 使用 `rpx` 单位而不是 `px`

## 🔗 相关文档

- [uni-app 官方文档](https://uniapp.dcloud.net.cn/)
- [小程序迁移报告.md](../小程序迁移报告.md)
- [小程序迁移-项目结构说明.md](./小程序迁移-项目结构说明.md)
- [小程序迁移-检查清单.md](./小程序迁移-检查清单.md)




