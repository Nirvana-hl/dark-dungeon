# 前后端连接情况报告

> 生成时间：2025-01-XX  
> 检查范围：前端 API 调用 vs 后端 Controller 实现

---

## 📊 总体情况

- **已连接功能**：8 个模块
- **部分连接功能**：5 个模块
- **未连接功能**：6 个模块

---

## ✅ 已完全连接的功能模块

### 1. 认证模块 ✅
**前端调用**：
- `POST /auth/login` - 用户登录
- `POST /auth/register` - 用户注册
- `POST /auth/logout` - 用户登出（前端调用，后端未实现）

**后端实现**：
- ✅ `POST /auth/login` - 已实现
- ✅ `POST /auth/register` - 已实现
- ❌ `POST /auth/logout` - **未实现**（前端会调用，但后端无此接口）

**状态**：基本连接成功，但缺少 logout 接口

---

### 2. 营地仪表盘模块 ✅
**前端调用**：
- `GET /camp/dashboard` - 获取营地聚合数据

**后端实现**：
- ✅ `GET /camp/dashboard` - 已实现

**状态**：✅ **完全连接成功**

---

### 3. 技能系统模块 ✅
**前端调用**：
- `GET /skills/{playerCharacterCode}` - 获取职业技能树
- `GET /user-skills` - 获取已解锁技能
- `POST /user-skills/unlock` - 解锁技能

**后端实现**：
- ✅ `GET /skills/{Code}` - 已实现（路径参数名不同：Code vs playerCharacterCode）
- ✅ `GET /user-skills` - 已实现
- ✅ `POST /user-skills/unlock` - 已实现

**状态**：✅ **完全连接成功**（注意路径参数名差异）

---

### 4. 商城模块 ✅
**前端调用**：
- `GET /camp/shop-offers` - 获取商城商品（前端实际调用）
- `POST /camp/purchase` - 购买商品（前端实际调用）

**后端实现**：
- ✅ `GET /shop/offers` - 已实现（路径不同：/shop/offers vs /camp/shop-offers）
- ✅ `POST /shop/purchase` - 已实现（路径不同：/shop/purchase vs /camp/purchase）

**状态**：⚠️ **路径不匹配** - 前端调用 `/camp/shop-offers`，后端实现 `/shop/offers`

---

### 5. 背包模块 ✅
**前端调用**：
- `GET /camp/inventory` - 获取背包物品
- `POST /camp/use-item` - 使用物品

**后端实现**：
- ✅ `GET /api/inventory` - 已实现（路径不同：/api/inventory vs /camp/inventory）
- ✅ `POST /api/inventory/use` - 已实现（路径不同：/api/inventory/use vs /camp/use-item）

**状态**：⚠️ **路径不匹配** - 前端调用 `/camp/*`，后端实现 `/api/*`

---

### 6. 成就系统模块 ✅
**前端调用**（api.ts 中定义）：
- `GET /achievement/list` - 获取成就列表
- `GET /achievement/progress` - 获取成就进度
- `POST /achievement/unlock` - 解锁成就

**后端实现**：
- ✅ `GET /achievements` - 已实现（路径不同）
- ✅ `GET /achievements/user` - 已实现（带用户完成状态）
- ✅ `GET /achievements/category/{category}` - 已实现
- ✅ `GET /achievements/{id}` - 已实现
- ✅ `GET /achievements/search` - 已实现
- ✅ `GET /achievements/user/category/{category}` - 已实现
- ✅ `GET /achievements/user/stats` - 已实现

**状态**：⚠️ **路径不匹配** - 前端定义 `/achievement/*`，后端实现 `/achievements/*`（注意单复数）

---

### 7. 地牢探索模块 ✅
**前端调用**（api.ts 中定义，但实际未使用）：
- `GET /dungeon/list` - 获取地牢列表
- `POST /dungeon/start` - 开始探索
- `POST /dungeon/end` - 结束探索

**后端实现**：
- ✅ `GET /dungeons` - 已实现（路径不同：/dungeons vs /dungeon/list）
- ✅ `GET /dungeons/runs/current` - 已实现
- ✅ `GET /dungeons/runs/{runId}` - 已实现
- ✅ `POST /dungeons/runs/start` - 已实现（路径不同：/dungeons/runs/start vs /dungeon/start）
- ✅ `POST /dungeons/runs/{runId}/explore` - 已实现
- ✅ `POST /dungeons/runs/{runId}/battle` - 已实现
- ✅ `POST /dungeons/runs/{runId}/end` - 已实现（路径不同：/dungeons/runs/{runId}/end vs /dungeon/end）

**状态**：⚠️ **路径不匹配且前端未实际调用** - 前端定义了接口但未在代码中使用

---

### 8. 卡牌系统模块 ✅
**前端调用**：
- `GET /card/user-cards` - 获取用户卡牌（gameApi.getUserCards）
- `GET /cards` - 获取卡牌列表（可能未使用）

**后端实现**：
- ✅ `GET /cards` - 已实现
- ✅ `GET /cards/{id}` - 已实现
- ✅ `POST /cards` - 已实现
- ✅ `PUT /cards/{id}` - 已实现
- ✅ `DELETE /cards/{id}` - 已实现
- ✅ `GET /user-cards` - 已实现（路径不同：/user-cards vs /card/user-cards）
- ✅ `GET /user-cards/equipped` - 已实现
- ✅ `GET /user-cards/{id}` - 已实现
- ✅ `POST /user-cards` - 已实现
- ✅ `PUT /user-cards/{id}` - 已实现
- ✅ `DELETE /user-cards/{id}` - 已实现

**状态**：⚠️ **路径不匹配** - 前端调用 `/card/user-cards`，后端实现 `/user-cards`

---

## ⚠️ 部分连接的功能模块

### 9. 角色管理模块 ⚠️
**前端调用**（characters.ts）：
- `GET /character/player/instance` - 获取玩家角色实例
- `GET /character/card/instance` - 获取卡牌角色实例
- `GET /character/card` - 获取卡牌角色模板

**后端实现**：
- ✅ `GET /user-player-characters` - 已实现（路径不同）
- ✅ `GET /user-player-characters/current` - 已实现
- ✅ `GET /user-player-characters/{id}` - 已实现
- ✅ `POST /user-player-characters` - 已实现
- ✅ `PUT /user-player-characters/{id}` - 已实现
- ✅ `GET /user-card-characters` - 已实现（路径不同）
- ✅ `GET /user-card-characters/deployed` - 已实现
- ✅ `GET /user-card-characters/{id}` - 已实现
- ✅ `POST /user-card-characters` - 已实现
- ✅ `PUT /user-card-characters/{id}` - 已实现
- ✅ `GET /card-characters` - 已实现（路径不同：/card-characters vs /character/card）
- ✅ `GET /player-characters` - 已实现（路径不同：/player-characters vs /character/player）

**状态**：⚠️ **路径不匹配** - 前端调用 `/character/*`，后端实现 `/user-*-characters` 和 `/*-characters`

---

### 10. 游戏战斗模块 ⚠️
**前端调用**（game.ts）：
- `GET /character/traits` - 获取角色特性
- `GET /card/user-cards` - 获取用户卡牌
- `GET /card/enemy-cards?stage={stageNum}&difficulty={difficulty}` - 获取敌方卡牌

**后端实现**：
- ❌ `GET /character/traits` - **未实现**
- ✅ `GET /user-cards` - 已实现（路径不同）
- ❌ `GET /card/enemy-cards` - **未实现**

**状态**：⚠️ **部分未实现** - 角色特性和敌方卡牌接口缺失

---

### 11. 营地其他功能 ⚠️
**前端调用**（campApi，但部分未实际使用）：
- `GET /camp/player-character` - 获取玩家角色（未使用，已通过 dashboard 获取）
- `GET /camp/card-characters` - 获取卡牌角色（未使用）
- `POST /camp/deploy-card-character` - 部署卡牌角色（已使用）
- `GET /camp/events` - 获取任务事件（未使用）
- `POST /camp/complete-event` - 完成任务事件（未使用）
- `GET /camp/ai-suggestions` - 获取AI建议（未使用）
- `POST /camp/refresh-ai-suggestions` - 刷新AI建议（未使用）

**后端实现**：
- ❌ `GET /camp/player-character` - **未实现**
- ❌ `GET /camp/card-characters` - **未实现**
- ❌ `POST /camp/deploy-card-character` - **未实现**
- ❌ `GET /camp/events` - **未实现**
- ❌ `POST /camp/complete-event` - **未实现**
- ❌ `GET /camp/ai-suggestions` - **未实现**
- ❌ `POST /camp/refresh-ai-suggestions` - **未实现**

**状态**：⚠️ **大部分未实现** - 只有 dashboard 接口可用

---

### 12. 钱包模块 ⚠️
**前端调用**（wallet.ts）：
- `GET /wallet/info` - 获取钱包信息
- `POST /wallet/add` - 增加货币
- `POST /wallet/consume` - 消费货币

**后端实现**：
- ❌ 钱包相关接口 **未实现**

**状态**：❌ **完全未实现**

---

### 13. 压力系统模块 ⚠️
**前端调用**（CampModern.vue）：
- `GET /stress/status` - 获取压力状态
- `GET /stress/debuffs` - 获取压力debuff
- `POST /stress/relieve` - 缓解压力

**后端实现**：
- ❌ 压力系统接口 **未实现**

**状态**：❌ **完全未实现**

---

## ❌ 未连接的功能模块

### 14. 统计模块 ❌
**前端调用**（api.ts 中定义，但未实际使用）：
- `GET /statistics/core-metrics` - 获取核心指标
- `GET /statistics/combat-stats` - 获取战斗统计
- `GET /statistics/resource-stats` - 获取资源统计
- `GET /statistics/achievement-stats` - 获取成就统计
- `GET /statistics/recent-activity` - 获取最近活动

**后端实现**：
- ❌ 统计模块接口 **未实现**

**状态**：❌ **完全未实现**

---

### 15. 用户信息模块 ❌
**前端调用**（api.ts 中定义）：
- `GET /user/info` - 获取用户信息
- `PUT /user/update` - 更新用户信息

**后端实现**：
- ❌ 用户信息接口 **未实现**

**状态**：❌ **完全未实现**

---

### 16. 卡牌装备模块 ❌
**前端调用**（api.ts 中定义）：
- `POST /card/equip` - 装备卡牌
- `POST /card/unequip` - 卸下卡牌
- `POST /card/upgrade` - 升级卡牌

**后端实现**：
- ❌ 卡牌装备接口 **未实现**（UserCard 实体有 equippedToUserCardCharacterId 字段，但无对应接口）

**状态**：❌ **完全未实现**

---

### 17. 道具模板模块 ❌
**前端调用**（api.ts 中定义）：
- `GET /item/list` - 获取道具模板列表

**后端实现**：
- ✅ `GET /api/items` - 已实现（路径不同：/api/items vs /item/list）

**状态**：⚠️ **路径不匹配**

---

### 18. 关卡进度模块 ❌
**前端调用**（可能未使用）：
- 无明确的前端调用

**后端实现**：
- ✅ `GET /user-stage-progress` - 已实现
- ✅ `GET /user-stage-progress/{stageNumber}` - 已实现
- ✅ `GET /user-stage-progress/chapter/{chapterNumber}` - 已实现
- ✅ `POST /user-stage-progress/{stageNumber}/pass` - 已实现
- ✅ `POST /user-stage-progress/{stageNumber}/attempt` - 已实现
- ✅ `GET /user-stage-progress/stats` - 已实现

**状态**：⚠️ **后端已实现，但前端未调用**

---

## 📋 路径不匹配问题汇总

| 前端路径 | 后端路径 | 状态 |
|---------|---------|------|
| `/camp/shop-offers` | `/shop/offers` | ❌ 不匹配 |
| `/camp/purchase` | `/shop/purchase` | ❌ 不匹配 |
| `/camp/inventory` | `/api/inventory` | ❌ 不匹配 |
| `/camp/use-item` | `/api/inventory/use` | ❌ 不匹配 |
| `/achievement/*` | `/achievements/*` | ❌ 不匹配（单复数） |
| `/dungeon/*` | `/dungeons/*` | ❌ 不匹配（单复数） |
| `/card/user-cards` | `/user-cards` | ❌ 不匹配 |
| `/character/player/instance` | `/user-player-characters` | ❌ 不匹配 |
| `/character/card/instance` | `/user-card-characters` | ❌ 不匹配 |
| `/character/card` | `/card-characters` | ❌ 不匹配 |
| `/item/list` | `/api/items` | ❌ 不匹配 |

---

## 🔧 需要修复的问题

### 高优先级（影响核心功能）

1. **商城接口路径不匹配**
   - 前端：`/camp/shop-offers` → 后端：`/shop/offers`
   - 前端：`/camp/purchase` → 后端：`/shop/purchase`
   - **影响**：商城功能无法使用

2. **背包接口路径不匹配**
   - 前端：`/camp/inventory` → 后端：`/api/inventory`
   - 前端：`/camp/use-item` → 后端：`/api/inventory/use`
   - **影响**：背包功能无法使用

3. **卡牌接口路径不匹配**
   - 前端：`/card/user-cards` → 后端：`/user-cards`
   - **影响**：游戏战斗中的用户卡牌加载失败

4. **角色特性接口缺失**
   - 前端：`GET /character/traits` → 后端：**未实现**
   - **影响**：战斗系统无法获取角色特性

5. **敌方卡牌接口缺失**
   - 前端：`GET /card/enemy-cards` → 后端：**未实现**
   - **影响**：战斗系统无法加载敌方卡牌

### 中优先级（影响辅助功能）

6. **钱包系统完全未实现**
   - 前端：`/wallet/*` → 后端：**未实现**
   - **影响**：金币系统无法使用

7. **压力系统完全未实现**
   - 前端：`/stress/*` → 后端：**未实现**
   - **影响**：压力缓解功能无法使用

8. **营地其他功能未实现**
   - 部署卡牌角色、任务事件、AI建议等
   - **影响**：营地部分功能缺失

### 低优先级（未实际使用）

9. **统计模块未实现**
   - 前端定义了接口但未使用

10. **用户信息模块未实现**
    - 前端定义了接口但未使用

11. **卡牌装备模块未实现**
    - 前端定义了接口但未使用

---

## 📝 建议修复方案

### 方案一：修改后端路径（推荐）
统一后端路径，使其与前端调用一致：
- `/shop/offers` → `/camp/shop-offers`
- `/api/inventory` → `/camp/inventory`
- `/user-cards` → `/card/user-cards`
- 等等...

### 方案二：修改前端路径
统一前端路径，使其与后端实现一致：
- `/camp/shop-offers` → `/shop/offers`
- `/camp/inventory` → `/api/inventory`
- `/card/user-cards` → `/user-cards`
- 等等...

### 方案三：添加路径映射
在后端添加路径别名，同时支持两种路径：
- 使用 `@RequestMapping` 的 `value` 数组支持多个路径

---

## ✅ 总结

### 已连接成功（8个模块）
1. ✅ 认证模块（缺少 logout）
2. ✅ 营地仪表盘
3. ✅ 技能系统
4. ✅ 商城模块（路径不匹配）
5. ✅ 背包模块（路径不匹配）
6. ✅ 成就系统（路径不匹配）
7. ✅ 地牢探索（路径不匹配，前端未使用）
8. ✅ 卡牌系统（路径不匹配）

### 部分连接（5个模块）
9. ⚠️ 角色管理（路径不匹配）
10. ⚠️ 游戏战斗（部分接口缺失）
11. ⚠️ 营地其他功能（大部分未实现）
12. ⚠️ 钱包模块（完全未实现）
13. ⚠️ 压力系统（完全未实现）

### 未连接（6个模块）
14. ❌ 统计模块
15. ❌ 用户信息模块
16. ❌ 卡牌装备模块
17. ❌ 道具模板模块（路径不匹配）
18. ❌ 关卡进度模块（后端已实现，前端未调用）

---

**最后更新**：2025-01-XX  
**检查人**：AI Assistant

