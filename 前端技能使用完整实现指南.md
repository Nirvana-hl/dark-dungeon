# 前端技能使用完整实现指南

## 📋 概述

本文档详细说明前端如何调用接口实现技能效果，包括完整的流程、代码示例和注意事项。

---

## 🔄 完整流程

### 流程概览

```
1. 战斗开始时
   └─> 加载可用技能列表（GET /user-skills/battle/{playerCharacterCode}/active）
   
2. 玩家回合
   └─> 显示可用技能按钮（根据当前法力值过滤）
   
3. 玩家点击技能
   └─> 验证技能（POST /user-skills/{skillId}/use）
   └─> 消耗法力值
   └─> 解析 effectPayload
   └─> 应用技能效果（复用卡牌效果逻辑）
   └─> 显示技能使用反馈
```

---

## 📝 详细实现步骤

### 第一步：添加技能API接口

**文件：** `frontend/api/request.ts`

在 `skillApi` 对象中添加以下方法：

```typescript
export const skillApi = {
  // ... 现有方法 ...
  
  /**
   * 获取战斗可用技能列表
   * GET /user-skills/battle/{playerCharacterCode}
   */
  async getBattleSkills(playerCharacterCode: string) {
    return await apiClient.get(`/user-skills/battle/${playerCharacterCode}`)
  },
  
  /**
   * 获取战斗可用主动技能列表
   * GET /user-skills/battle/{playerCharacterCode}/active
   */
  async getActiveSkillsForBattle(playerCharacterCode: string) {
    return await apiClient.get(`/user-skills/battle/${playerCharacterCode}/active`)
  },
  
  /**
   * 获取技能详细信息
   * GET /user-skills/{skillId}/info
   */
  async getSkillInfo(skillId: number | string) {
    return await apiClient.get(`/user-skills/${skillId}/info`)
  },
  
  /**
   * 验证并使用技能
   * POST /user-skills/{skillId}/use
   */
  async useSkill(skillId: number | string, currentMana: number) {
    return await apiClient.post(`/user-skills/${skillId}/use`, {
      currentMana
    })
  }
}
```

---

### 第二步：在Game Store中添加技能状态和方法

**文件：** `frontend/stores/game.ts`

#### 2.1 添加技能状态

```typescript
export const useGameStore = defineStore('game', () => {
  // ... 现有状态 ...
  
  // 技能相关状态
  const availableSkills = ref<Array<{
    id: number
    name: string
    description: string
    effectPayload: string
    skillType: 'active' | 'passive'
    manaCost?: number
  }>>([])
  
  const skillsLoaded = ref(false)
  
  // ... 其他状态 ...
})
```

#### 2.2 添加加载技能方法

```typescript
/**
 * 加载战斗可用技能
 */
async function loadBattleSkills(playerCharacterCode: string) {
  if (skillsLoaded.value) return // 已加载，跳过
  
  try {
    const response = await skillApi.getActiveSkillsForBattle(playerCharacterCode)
    if (response.data.code === 200 && Array.isArray(response.data.data)) {
      // 解析每个技能的法力值消耗
      availableSkills.value = response.data.data.map((skill: any) => {
        let manaCost = 1 // 默认消耗1点
        if (skill.effectPayload) {
          try {
            const effect = JSON.parse(skill.effectPayload)
            manaCost = effect.mana_cost ?? effect.action_cost ?? 1
          } catch {
            // 解析失败，使用默认值
          }
        }
        return {
          id: skill.id,
          name: skill.name,
          description: skill.description,
          effectPayload: skill.effectPayload,
          skillType: skill.skillType || 'active',
          manaCost
        }
      })
      skillsLoaded.value = true
      log(`已加载 ${availableSkills.value.length} 个可用技能`)
    }
  } catch (e: any) {
    log('加载技能失败：' + e?.message)
    availableSkills.value = []
  }
}
```

#### 2.3 添加使用技能方法

```typescript
/**
 * 使用技能
 * @param skillId 技能ID
 */
async function useSkill(skillId: number) {
  if (turn.value !== 'player') {
    log('提示：只能在玩家回合使用技能')
    return false
  }
  
  if (battleOver.value) {
    log('提示：战斗已结束')
    return false
  }
  
  // 查找技能信息
  const skill = availableSkills.value.find(s => s.id === skillId)
  if (!skill) {
    log('错误：技能不存在')
    return false
  }
  
  // 检查法力值
  const manaCost = skill.manaCost || 1
  if (mana.value < manaCost) {
    log(`提示：法力值不足，需要 ${manaCost} 点，当前只有 ${mana.value} 点`)
    return false
  }
  
  try {
    // 调用API验证技能
    const response = await skillApi.useSkill(skillId, mana.value)
    
    if (response.data.code !== 200) {
      log('错误：' + (response.data.message || '技能使用失败'))
      return false
    }
    
    const skillData = response.data.data
    if (!skillData) {
      log('错误：技能数据为空')
      return false
    }
    
    // 消耗法力值
    mana.value -= skillData.manaCost
    
    // 播放技能使用音效
    soundManager.playSound(SoundType.PLAY_CARD)
    
    // 解析并应用技能效果
    if (skillData.effectPayload) {
      applySkillEffect(skillData.effectPayload, skillData.skillName, skillData.target)
    }
    
    log(`使用技能：${skillData.skillName}（消耗 ${skillData.manaCost} 点法力值）`)
    
    return true
  } catch (e: any) {
    log('使用技能失败：' + e?.message)
    return false
  }
}

/**
 * 应用技能效果
 * 复用卡牌效果应用逻辑
 * @param effectPayload 技能效果配置（JSON字符串）
 * @param skillName 技能名称
 * @param target 目标类型
 */
function applySkillEffect(effectPayload: string, skillName: string, target?: string) {
  if (!effectPayload) return
  
  try {
    const effect = JSON.parse(effectPayload)
    const effectType = effect.type || ''
    
    // 根据技能类型应用效果
    switch (effectType) {
      case 'heal':
      case 'heal_single':
        // 治疗效果
        applyHealEffect(effect, skillName, target)
        break
        
      case 'attack':
      case 'damage_single':
        // 伤害效果
        applyDamageEffect(effect, skillName, target)
        break
        
      case 'buff':
        // 增益效果
        applyBuffEffect(effect, skillName, target)
        break
        
      case 'debuff':
        // 减益效果
        applyDebuffEffect(effect, skillName, target)
        break
        
      default:
        log(`技能 ${skillName} 的效果类型 ${effectType} 暂未实现`)
    }
  } catch (e: any) {
    log(`技能效果解析失败：${skillName}，错误：${e?.message}`)
  }
}

/**
 * 应用治疗效果
 */
function applyHealEffect(effect: any, skillName: string, target?: string) {
  const healAmount = effect.heal_amount ?? effect.heal ?? 0
  if (healAmount <= 0) return
  
  const actualTarget = target || effect.target || 'hero'
  
  if (actualTarget === 'hero' || actualTarget === 'ally' || actualTarget === 'self') {
    // 治疗英雄
    const before = heroHP.value
    heroHP.value = Math.min(heroHP.value + healAmount, 100) // 假设最大HP为100
    const actual = heroHP.value - before
    if (actual > 0) {
      soundManager.playSound(SoundType.HEAL, { volume: 0.4 })
      log(`技能 ${skillName}：恢复 ${actual} 点生命值（${before} → ${heroHP.value}）`)
    }
  } else if (actualTarget === 'all_allies') {
    // 治疗所有友方（包括英雄和战场上的角色）
    soundManager.playSound(SoundType.HEAL, { volume: 0.4 })
    
    // 治疗英雄
    const before = heroHP.value
    heroHP.value = Math.min(heroHP.value + healAmount, 100)
    const heroHeal = heroHP.value - before
    
    // 治疗战场上的角色
    let minionHealCount = 0
    board.value.forEach(minion => {
      const before = minion.health
      minion.health += healAmount
      if (minion.health > before) minionHealCount++
    })
    
    log(`技能 ${skillName}：全体恢复 ${healAmount} 点生命值（英雄：+${heroHeal}，角色：${minionHealCount}个）`)
  }
}

/**
 * 应用伤害效果
 */
function applyDamageEffect(effect: any, skillName: string, target?: string) {
  const damage = effect.damage ?? 0
  if (damage <= 0) return
  
  const actualTarget = target || effect.target || 'enemy'
  
  if (actualTarget === 'enemy') {
    // 对敌人造成伤害
    const before = enemyHP.value
    enemyHP.value = Math.max(0, enemyHP.value - damage)
    const actual = before - enemyHP.value
    if (actual > 0) {
      log(`技能 ${skillName}：对敌人造成 ${actual} 点伤害（${before} → ${enemyHP.value}）`)
      checkVictory() // 检查胜负
    }
  } else if (actualTarget === 'all_enemies') {
    // 对所有敌人造成伤害（包括敌人本体和战场上的敌人）
    log(`技能 ${skillName}：对所有敌人造成 ${damage} 点伤害`)
    
    // 对敌人本体造成伤害
    const before = enemyHP.value
    enemyHP.value = Math.max(0, enemyHP.value - damage)
    const actual = before - enemyHP.value
    if (actual > 0) {
      log(`  - 敌人本体：${before} → ${enemyHP.value}（-${actual}）`)
      checkVictory()
    }
    
    // 对敌人战场上的角色造成伤害
    enemyBoard.value.forEach(minion => {
      const before = minion.health
      minion.health = Math.max(0, minion.health - damage)
      const actual = before - minion.health
      if (actual > 0) {
        log(`  - ${minion.name}：${before} → ${minion.health}（-${actual}）`)
      }
    })
  }
}

/**
 * 应用增益效果
 */
function applyBuffEffect(effect: any, skillName: string, target?: string) {
  const actualTarget = target || effect.target || 'self'
  
  if (effect.armor_bonus) {
    // 护甲加成
    log(`技能 ${skillName}：提供 ${effect.armor_bonus} 点护甲`)
    // 这里可以添加到英雄的护甲值（如果有的话）
  }
  
  if (effect.damage_reduction) {
    // 伤害减免
    const reduction = (effect.damage_reduction * 100).toFixed(0)
    log(`技能 ${skillName}：减少 ${reduction}% 受到的伤害`)
    // 这里可以添加到英雄的buff列表（如果有的话）
  }
  
  // 其他buff效果...
}

/**
 * 应用减益效果
 */
function applyDebuffEffect(effect: any, skillName: string, target?: string) {
  const actualTarget = target || effect.target || 'enemy'
  
  if (effect.attack_reduction) {
    // 降低敌人攻击力
    const reduction = (effect.attack_reduction * 100).toFixed(0)
    log(`技能 ${skillName}：降低敌人 ${reduction}% 攻击力`)
    // 这里可以添加到敌人的debuff列表（如果有的话）
  }
  
  // 其他debuff效果...
}

// 导出方法
return {
  // ... 现有导出 ...
  availableSkills,
  skillsLoaded,
  loadBattleSkills,
  useSkill
}
```

---

### 第三步：在战斗界面显示技能按钮

**文件：** `frontend/pages/battle/battle.vue`

#### 3.1 添加技能相关状态

```typescript
<script setup lang="ts">
// ... 现有导入 ...
import { skillApi } from '@/api/request'

// ... 现有状态 ...

// 技能相关状态
const availableSkills = computed(() => game.availableSkills)
const skillsLoaded = computed(() => game.skillsLoaded)
const selectedSkill = ref<{ id: number; name: string; description: string; manaCost: number } | null>(null)

// ... 其他状态 ...
</script>
```

#### 3.2 在战斗开始时加载技能

```typescript
// 在 onLoad 或战斗初始化时
onLoad(async (options) => {
  routeQuery = options || {}
  
  // ... 现有初始化代码 ...
  
  // 加载技能（需要玩家职业代码）
  const campStore = useCampStore()
  const playerChar = campStore.playerCharacter
  if (playerChar?.playerCharacterCode) {
    await game.loadBattleSkills(playerChar.playerCharacterCode)
  }
})
```

#### 3.3 添加技能使用函数

```typescript
/**
 * 使用技能
 */
async function onUseSkill(skillId: number) {
  const success = await game.useSkill(skillId)
  if (success) {
    selectedSkill.value = null // 关闭技能详情面板
  }
}

/**
 * 显示技能详情
 */
function showSkillDetails(skill: typeof availableSkills.value[0]) {
  selectedSkill.value = {
    id: skill.id,
    name: skill.name,
    description: skill.description,
    manaCost: skill.manaCost || 1
  }
}

/**
 * 关闭技能详情
 */
function closeSkillDetails() {
  selectedSkill.value = null
}
```

#### 3.4 在模板中添加技能按钮区域

```vue
<template>
  <!-- ... 现有内容 ... -->
  
  <!-- 技能按钮区域（放在手牌区域上方或下方） -->
  <view class="skills-section" v-if="availableSkills.length > 0">
    <view class="skills-title">可用技能</view>
    <view class="skills-list">
      <view
        v-for="skill in availableSkills"
        :key="skill.id"
        class="skill-button"
        :class="{
          'affordable': mana >= (skill.manaCost || 1),
          'unaffordable': mana < (skill.manaCost || 1)
        }"
        @click="showSkillDetails(skill)"
      >
        <view class="skill-cost">{{ skill.manaCost || 1 }}</view>
        <view class="skill-name">{{ skill.name }}</view>
      </view>
    </view>
  </view>
  
  <!-- 技能详情面板（类似卡牌详情面板） -->
  <view v-if="selectedSkill" class="skill-details">
    <view class="skill-details-header">
      <text class="skill-details-title">技能详情</text>
      <button class="skill-details-close" @click="closeSkillDetails">✕</button>
    </view>
    <view class="skill-details-body">
      <view class="skill-details-name">{{ selectedSkill.name }}</view>
      <view class="skill-details-cost">消耗：{{ selectedSkill.manaCost }} 点法力值</view>
      <view class="skill-details-description">{{ selectedSkill.description }}</view>
      <view class="skill-details-actions">
        <button
          class="use-skill-btn"
          @click="onUseSkill(selectedSkill.id); closeSkillDetails()"
          :disabled="mana < selectedSkill.manaCost"
        >
          使用技能
        </button>
      </view>
    </view>
  </view>
  
  <!-- ... 其他内容 ... -->
</template>

<style scoped>
/* 技能按钮样式 */
.skills-section {
  padding: 10px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 8px;
  margin: 10px 0;
}

.skills-title {
  font-size: 14px;
  color: #fff;
  margin-bottom: 8px;
}

.skills-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.skill-button {
  position: relative;
  width: 80px;
  height: 100px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 8px;
  padding: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.2s;
}

.skill-button.affordable {
  opacity: 1;
  transform: scale(1);
}

.skill-button.unaffordable {
  opacity: 0.5;
  filter: grayscale(0.5);
}

.skill-button:active {
  transform: scale(0.95);
}

.skill-cost {
  position: absolute;
  top: 4px;
  right: 4px;
  background: rgba(0, 0, 0, 0.6);
  color: #fff;
  border-radius: 50%;
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: bold;
}

.skill-name {
  font-size: 12px;
  color: #fff;
  text-align: center;
  margin-top: 8px;
}

/* 技能详情面板样式 */
.skill-details {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 80%;
  max-width: 400px;
  background: #1a1a2e;
  border-radius: 12px;
  z-index: 1000;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
}

.skill-details-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.skill-details-title {
  font-size: 18px;
  font-weight: bold;
  color: #fff;
}

.skill-details-close {
  background: none;
  border: none;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
  padding: 0;
  width: 32px;
  height: 32px;
}

.skill-details-body {
  padding: 16px;
}

.skill-details-name {
  font-size: 20px;
  font-weight: bold;
  color: #fff;
  margin-bottom: 8px;
}

.skill-details-cost {
  font-size: 14px;
  color: #a0a0a0;
  margin-bottom: 12px;
}

.skill-details-description {
  font-size: 14px;
  color: #ccc;
  line-height: 1.6;
  margin-bottom: 16px;
}

.skill-details-actions {
  display: flex;
  justify-content: center;
}

.use-skill-btn {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  border: none;
  border-radius: 8px;
  padding: 12px 24px;
  font-size: 16px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.2s;
}

.use-skill-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.use-skill-btn:not(:disabled):active {
  transform: scale(0.95);
}
</style>
```

---

## 🔧 技能效果应用逻辑详解

### 技能效果类型

技能效果通过 `effectPayload` JSON 字段定义，支持以下类型：

#### 1. 治疗效果（heal）
```json
{
  "type": "heal",
  "target": "ally",
  "heal_amount": 25,
  "remove_debuff": true
}
```

**应用逻辑：**
- `target: "ally"` 或 `"hero"` 或 `"self"` → 治疗英雄
- `target: "all_allies"` → 治疗所有友方（英雄+战场角色）
- `heal_amount` → 治疗量
- `remove_debuff` → 是否移除负面状态

#### 2. 伤害效果（attack）
```json
{
  "type": "attack",
  "target": "enemy",
  "damage": 18,
  "damage_type": "shadow"
}
```

**应用逻辑：**
- `target: "enemy"` → 对敌人本体造成伤害
- `target: "all_enemies"` → 对所有敌人造成伤害（本体+战场角色）
- `damage` → 伤害值

#### 3. 增益效果（buff）
```json
{
  "type": "buff",
  "target": "ally",
  "duration": 3,
  "armor_bonus": 15,
  "damage_reduction": 0.1
}
```

**应用逻辑：**
- `armor_bonus` → 增加护甲值
- `damage_reduction` → 减少受到的伤害（百分比）
- `duration` → 持续回合数（需要实现buff系统）

#### 4. 减益效果（debuff）
```json
{
  "type": "debuff",
  "target": "enemy",
  "duration": 4,
  "attack_reduction": 0.25,
  "defense_reduction": 0.2
}
```

**应用逻辑：**
- `attack_reduction` → 降低攻击力（百分比）
- `defense_reduction` → 降低防御力（百分比）
- `duration` → 持续回合数（需要实现debuff系统）

---

## 📊 完整流程图

```
┌─────────────────────────────────────────────────────────┐
│ 战斗开始                                                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 1. 加载可用技能                                          │
│    GET /user-skills/battle/{playerCharacterCode}/active │
│    └─> 解析 effectPayload，提取 manaCost                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 2. 显示技能按钮                                          │
│    - 根据当前法力值过滤可用技能                          │
│    - 显示技能名称和消耗                                  │
│    - 法力值不足的技能显示为灰色                          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 3. 玩家点击技能                                          │
│    └─> 显示技能详情面板                                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 4. 玩家确认使用                                          │
│    └─> 调用 useSkill(skillId)                           │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 5. 验证技能（后端API）                                   │
│    POST /user-skills/{skillId}/use                      │
│    └─> 检查：已解锁、主动技能、法力值足够                 │
│    └─> 返回：skillName, manaCost, effectPayload, target │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 6. 消耗法力值                                            │
│    mana.value -= skillData.manaCost                     │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 7. 解析 effectPayload                                    │
│    const effect = JSON.parse(effectPayload)             │
│    const effectType = effect.type                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 8. 应用技能效果                                          │
│    ├─> heal → applyHealEffect()                          │
│    ├─> attack → applyDamageEffect()                      │
│    ├─> buff → applyBuffEffect()                          │
│    └─> debuff → applyDebuffEffect()                     │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 9. 显示反馈                                              │
│    - 播放音效                                            │
│    - 显示战斗日志                                        │
│    - 更新UI（HP、法力值等）                              │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 关键代码位置

### 1. API接口定义
- **文件：** `frontend/api/request.ts`
- **位置：** `skillApi` 对象（第553行附近）

### 2. 技能状态和方法
- **文件：** `frontend/stores/game.ts`
- **位置：** `useGameStore` 中（需要添加）

### 3. 战斗界面UI
- **文件：** `frontend/pages/battle/battle.vue`
- **位置：** 模板中添加技能按钮区域

---

## ⚠️ 注意事项

### 1. 技能效果应用

**重要：** 技能效果由前端自行应用，后端只返回效果信息。

**原因：**
- 战斗是在前端进行的（前端维护战斗状态）
- 后端只负责验证技能是否可以使用
- 前端需要解析 `effectPayload` 并应用效果

### 2. 效果应用逻辑

**建议：** 复用现有的卡牌效果应用逻辑

**参考：**
- `frontend/stores/game.ts` 中的卡牌效果处理
- 可以创建通用的 `applyEffect()` 函数，同时支持卡牌和技能

### 3. 目标类型

技能效果的目标类型：
- `"hero"` / `"ally"` / `"self"` → 英雄（玩家角色）
- `"enemy"` → 敌人本体
- `"all_allies"` → 所有友方（英雄+战场角色）
- `"all_enemies"` → 所有敌人（敌人本体+敌人战场角色）

### 4. 被动技能

**注意：** 被动技能不需要前端处理
- 被动技能由后端在战斗开始时自动应用
- 前端只需要显示被动技能的激活信息（如果有战斗日志）

---

## 📝 实现检查清单

### API接口
- [ ] 添加 `getBattleSkills()` 方法
- [ ] 添加 `getActiveSkillsForBattle()` 方法
- [ ] 添加 `getSkillInfo()` 方法
- [ ] 添加 `useSkill()` 方法

### Game Store
- [ ] 添加 `availableSkills` 状态
- [ ] 添加 `skillsLoaded` 状态
- [ ] 添加 `loadBattleSkills()` 方法
- [ ] 添加 `useSkill()` 方法
- [ ] 添加 `applySkillEffect()` 方法
- [ ] 添加 `applyHealEffect()` 方法
- [ ] 添加 `applyDamageEffect()` 方法
- [ ] 添加 `applyBuffEffect()` 方法
- [ ] 添加 `applyDebuffEffect()` 方法

### 战斗界面
- [ ] 在战斗开始时加载技能
- [ ] 显示技能按钮区域
- [ ] 实现技能详情面板
- [ ] 实现技能点击使用逻辑
- [ ] 根据法力值禁用/启用技能按钮
- [ ] 显示技能使用反馈

### 测试
- [ ] 测试技能加载
- [ ] 测试技能使用（法力值足够）
- [ ] 测试技能使用（法力值不足）
- [ ] 测试技能效果应用（治疗、伤害、buff、debuff）
- [ ] 测试技能使用后的UI更新

---

## 🔄 与卡牌使用的对比

| 功能 | 卡牌 | 技能 |
|------|------|------|
| 获取列表 | 从卡组加载 | API: `getActiveSkillsForBattle()` |
| 验证使用 | 前端检查法力值 | API: `useSkill()` 验证 |
| 消耗资源 | 前端直接扣除 | 前端扣除（API已验证） |
| 应用效果 | 前端解析 `effectPayload` | 前端解析 `effectPayload` |
| 显示反馈 | 战斗日志 | 战斗日志 |

**结论：** 技能使用流程与卡牌使用类似，主要区别是：
1. 技能需要从API加载列表
2. 技能使用前需要调用API验证
3. 效果应用逻辑完全相同

---

## 📚 相关文档

- `技能系统短期改进完成总结.md` - 后端功能说明
- `技能系统战斗实现分析报告.md` - 系统分析报告
- `backend/src/main/java/com/dungeon/controller/SkillController.java` - 后端API接口

---

**创建时间：** 2026-01-06  
**创建人员：** AI助手  
**文档版本：** v1.0

